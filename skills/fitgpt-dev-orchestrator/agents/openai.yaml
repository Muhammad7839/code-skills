# UI metadata for the fitgpt-dev-orchestrator skill.
interface:
  display_name: "FitGPT Dev Orchestrator"
  short_description: "Repeatable FitGPT safety, branch, env, and startup orchestrator"
  default_prompt: "Use $fitgpt-dev-orchestrator to inspect FitGPT context, read recent logs, and select only the relevant core skill for the next safe action."
